<h1>On commence !</h1>
<p>Voici l'ordre des participants :</p>
<ul>
	<% @members.each_with_index do |member, index| %>
		<li>Personne n°<%=index+1%> : <%= member.nickname %></li>
	<% end %>
</ul>

<%= button_to "Joueur suivant", new_random_soudain_path, method: :get %>
